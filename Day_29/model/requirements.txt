# Only needed to retrain the models -- see README.md. The deployed backend
# (backend/requirements.txt) has its own, much lighter, requirements file,
# since Model A needs none of these at serving time (Part A's deployment
# decision).
torch==2.13.0
transformers==4.57.6
datasets==5.0.1
scikit-learn==1.9.0
matplotlib==3.11.1
pandas==3.0.5
numpy==2.5.2
sentencepiece==0.2.2
joblib==1.5.3
pytest==9.1.1
httpx==0.28.1
