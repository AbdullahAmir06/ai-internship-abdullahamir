venv/
__pycache__/
*.pyc
data/
.claude/
*.log

# DistilBERT fine-tuned weights (~268MB, over GitHub's 100MB hard limit) --
# reproducible via `python part_c_transformer_classification.py`, see
# Day_26/README.md.
Day_26/models/distilbert_ag_news.pt

# Task 28 -- local secrets, never committed
Day_28/.env

# Day 29 capstone -- Model B (fine-tuned DistilBERT, ~268MB) is trained and
# evaluated but deliberately not deployed (see docs/part_a_scope_and_planning.md
# and FINAL_REPORT.md) and exceeds GitHub's 100MB hard limit -- reproducible
# via `python model/train_transformer.py`. The final state_dict is kept out
# of git; the best-epoch checkpoint (same reasoning) is too.
Day_29/model/artifacts/model_b_distilbert_final.pt
Day_29/model/artifacts/model_b_distilbert_checkpoint.pt
